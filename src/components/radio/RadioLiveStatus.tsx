import React from 'react';

const RadioLiveStatus: React.FC = () => (
  <span className="radio-live-status" aria-label="Transmissão ao vivo">
    <span className="radio-live-status-dot" aria-hidden="true" />
    Ao vivo
  </span>
);

export default RadioLiveStatus;
